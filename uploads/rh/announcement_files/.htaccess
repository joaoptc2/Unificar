# Anexos privados de comunicados: só são entregues pelo download autenticado
# do módulo (index.php?m=rh&page=files&action=get&...).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
