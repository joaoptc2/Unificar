# Módulo da plataforma unificada — executado APENAS via front controller
# raiz (/index.php?m=chat). Nenhum acesso web direto a este diretório.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
Options -Indexes
